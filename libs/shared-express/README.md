# shared-express

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build shared-express` to build the library.

## Running unit tests

Run `nx test shared-express` to execute the unit tests via [Jest](https://jestjs.io).
